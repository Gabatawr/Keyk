<Window x:Class="Keyk.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

        xmlns:cmd="clr-namespace:Keyk.Infrastructure.Commands"
        xmlns:vm="clr-namespace:Keyk.ViewModels"
        xmlns:l="clr-namespace:Keyk"

        WindowStyle="None" AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"

        Height="450" MinHeight="150"
        Width="800" MinWidth="150"

        Background="#F333">

    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <cmd:MoveApplicationCommand x:Key="MoveApplicationCommand"/>
    </Window.Resources>

    <Window.InputBindings>
        <MouseBinding MouseAction="LeftClick" Command="{StaticResource MoveApplicationCommand}"/>
    </Window.InputBindings>

    <Grid>

    </Grid>
</Window>
